{
  "name": "connparse-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm --filter connparse test",
    "test:js": "pnpm --filter connparse test",
    "check": "pnpm --filter connparse check",
    "check:js": "pnpm --filter connparse check"
  },
  "devDependencies": {}
}
